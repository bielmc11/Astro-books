---
import { SCORE_API_ENDPOINT } from "astro:env/server";

const res = await fetch(SCORE_API_ENDPOINT); //La api ya devuelve un numero
const scoreValue = await res.text();
---

<span id="score" class="text-xs opacity-80"
  >⭐ Puntuación: {scoreValue.trim()}/5
</span>
